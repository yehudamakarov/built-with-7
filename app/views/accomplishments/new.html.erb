<%= form_for @accomplishment do |f| %>
  What did you accomplish? <%= f.text_field :title %>
  <br>
  Was it a game changer?: <%= f.text_area :effect %>
  <br>
  Which day of the week was this done on? <%= f.collection_select :day, @user.days, :name, :name %>
  <br>
  At what time? <%= f.datetime_select :date_time, { discard_year: true, discard_month: true, minute_step: 15 }  %>
  <br>
  Do this every: <%= f.collection_check_boxes :day_ids, @user.days, :id, :name %>
  <br>
  <%= f.submit "Accomplished!" %>


<% end %>
