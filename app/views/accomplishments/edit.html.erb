<h1>Edit an Accomplishment</h1>

<%= form_for @accomplishment do |f| %>
  What's the accomplishment?
  <br>
  <%= f.text_field :title %>
  <br>
  Its effect:
  <br>
  <%= f.text_area :effect %>
  <br>
  At what time?
  <br>
  <%= f.datetime_select :date_time, { minute_step: 15 }  %>
  <br>
  Do this every:
  <br>
  <%= f.collection_check_boxes :day_ids, @user.days, :id, :name do |e| %>
    <div class="day-choice">
      <%= e.check_box %>
      <%= e.label %>
    </div>
  <% end %>
  <br>
  <%= f.submit "Update!" %>
<% end %>
